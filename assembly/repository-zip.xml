<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 https://maven.apache.org/xsd/assembly-1.1.3.xsd">
    <id>repository</id>
    <formats>
        <format>zip</format>
    </formats>

    <!-- Keep the archive self-contained and recognizable when unpacked. -->
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>${project.artifactId}-${project.version}</baseDirectory>

    <fileSets>
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>/</outputDirectory>
            <useDefaultExcludes>true</useDefaultExcludes>
            <excludes>
                <!-- VCS, CI, generated build output, editor metadata, and local scratch files. -->
                <exclude>.git/**</exclude>
                <exclude>.github/**</exclude>
                <exclude>.claude/**</exclude>
                <exclude>assembly/utl_interval-build.properties</exclude>
                <exclude>target/**</exclude>
                <exclude>.idea/**</exclude>
                <exclude>.vscode/**</exclude>
                <exclude>*.iml</exclude>
                <exclude>**/*.log</exclude>
                <exclude>*.tmp</exclude>
                <exclude>*.temp</exclude>
                <exclude>*.bak</exclude>
                <exclude>*.swp</exclude>
                <exclude>*~</exclude>
            </excludes>
        </fileSet>
    </fileSets>

    <files>
        <file>
            <source>${project.build.directory}/generated-build-metadata/META-INF/utl_interval-build.properties</source>
            <outputDirectory>META-INF</outputDirectory>
            <destName>utl_interval-build.properties</destName>
        </file>
    </files>
</assembly>
